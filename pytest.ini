[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests/unit tests/integration
markers =
    integration: Docker-based integration tests (set RUN_INTEGRATION=1)
addopts = -p no:pytest_homeassistant_custom_component
